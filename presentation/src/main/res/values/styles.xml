<resources>
    <!-- TextViews -->
    <style name="MTextView" parent="TextAppearance.AppCompat">
        <item name="android:textColorLink">@color/colorAccent</item>
        <item name="android:fontFamily">monospace</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="MTextView.White" parent="MTextView">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="MTextView.Black" parent="MTextView">
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="MTextView.Titles" parent="MTextView">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="MTextView.BlackTitles" parent="MTextView.Titles">
        <item name="android:textColor">@android:color/black</item>
    </style>

    <!-- ProgressBar -->
    <style name="MProgressBar" parent="Widget.AppCompat.ProgressBar">
        <item name="android:progressTint">@color/colorAccent</item>
        <item name="android:indeterminateTint">@color/colorAccent</item>
    </style>

    <!-- RecyclerView -->
    <style name="MRecyclerView" parent="">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:fadeScrollbars">true</item>
        <item name="android:scrollbarStyle">outsideOverlay</item>
        <item name="android:overScrollMode">ifContentScrolls</item>
        <item name="android:layout_margin">8dp</item>
        <item name="layoutManager">androidx.recyclerview.widget.LinearLayoutManager</item>
    </style>

    <style name="MRecyclerView.Vertical">
        <item name="android:orientation">vertical</item>
        <item name="android:scrollbars">vertical</item>
    </style>

</resources>